# xmip-core-message-fixed-width

Fixed-width shape: lines as numbered parts, or each line cut into the positions the caller names, no announced type. A technology of [xmip-core-message](https://github.com/IlleNilsson/xmip-core-message).

## Toolchain

`rust-toolchain.toml` pins the toolchain for the whole estate. Do not change it
here.

## Verification

The included workflow is manual-only and calls the versioned shared workflow at
`IlleNilsson/.github@v1`.
